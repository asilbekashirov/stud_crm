import { INews } from "../../models/news"
import { FC } from "react"

const NewsCard: FC<INews> = ({name, image}) => {
    return (
        <div>
            
        </div>
    )
}

export default NewsCard